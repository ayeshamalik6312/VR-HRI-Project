using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class chipped : MonoBehaviour
{
    public bool isChipped = true;
    // Start is called before the first frame update
    void Start()
    {
        isChipped = true;
    }

}
